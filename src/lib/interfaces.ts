/** Describes interface settings for color page */
export interface ColorSettings {
    showGap: boolean
}

export interface IPalette {
    name: string
    mainColor: string
    hueRotation: number
    colors: string[]
}
